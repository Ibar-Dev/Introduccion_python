# Creación de conjuntos
colores = {"rojo", "verde", "azul"}
numeros = {1, 2, 3, 4, 5}

# Operaciones de conjunto
union = colores.union(numeros)
interseccion = colores.intersection(numeros)
diferencia = colores.difference(numeros)
