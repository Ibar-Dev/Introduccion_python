# Creación de tuplas
punto = (10, 20)
coordenadas = (0, 0, 1)

# Acceso a elementos de la tupla
x = punto[0]
y = punto[1]

# Las tuplas son inmutables, no se pueden modificar
# punto[0] = 20  # Esto generaría un error
